services:
  server:
    build: .
    ports:
      - "3000:3000"
      - "40000:40000/udp"
      - "40000:40000/tcp"
    environment:
      DATABASE_URL: postgres://ecto:ecto@db:5432/ecto
      DATABASE_TYPE: pg
      JWT_SECRET: change-me
      # SERVER_ADDRESS: your-public-ip  # Set to your server's public IP for remote voice/video
    depends_on:
      db:
        condition: service_healthy
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ecto
      POSTGRES_PASSWORD: ecto
      POSTGRES_DB: ecto
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ecto"]
      interval: 5s
      timeout: 5s
      retries: 5
volumes:
  pgdata:
